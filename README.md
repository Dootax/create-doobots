# create-doobots

Uma CLI para criar projetos para serem executados no DooBots com um único comando.

Para iniciar, use um dos comandos abaixo:

- `npx doobots@latest`
- `npm init doobots@latest`
- `yarn create doobots`
- `pnpm create doobots`